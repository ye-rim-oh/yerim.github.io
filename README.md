# yerim.github.io

GitHub Pages site powered by Jekyll + Minimal Mistakes (`contrast` skin).

## Local preview (optional)

```bash
bundle install
bundle exec jekyll serve
```

Then open `http://127.0.0.1:4000`.
